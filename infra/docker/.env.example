# Copy to infra/docker/.env and adjust values as needed.

# Ports
WEB_PORT=3000
API_PORT=3001
POSTGRES_PORT=5432
MAILDEV_SMTP_PORT=1025
MAILDEV_WEB_PORT=1080

# Postgres
POSTGRES_DB=commerce
POSTGRES_USER=commerce
POSTGRES_PASSWORD=commerce

# Stripe mode
# mock = no Stripe account required (local placeholder IDs)
# test = real Stripe test mode
STRIPE_MODE=mock
STRIPE_API_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_FORWARD_TO_PATH=/webhooks/stripe
STRIPE_PRODUCT_ID=
STRIPE_PRICE_ID=

# Stripe test setup defaults (used by infra/docker/scripts/stripe-setup-test-mode.sh)
STRIPE_PRODUCT_NAME=GenX Delay
STRIPE_PRODUCT_DESCRIPTION=GenX Delay VST3 one-time purchase
STRIPE_PRODUCT_SKU=genx-delay-vst3
STRIPE_PRICE_UNIT_AMOUNT=4900
STRIPE_PRICE_CURRENCY=usd

# Email provider
EMAIL_PROVIDER=maildev
